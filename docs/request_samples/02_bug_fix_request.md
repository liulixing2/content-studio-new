# Bug 修复需求模板

## 问题标题

```text
例如：点击“导入并套公众号模板”后正文没有进入预览区。
```

## 复现步骤

1. 打开页面：
2. 输入/选择：
3. 点击：
4. 实际出现：

## 当前结果

```text
这里粘贴错误提示、页面显示、控制台报错或接口返回。
```

## 期望结果

```text
例如：点击后正文应该进入正文预览区，且不会自动保存。
```

## 本次边界

只修这个 bug，不顺手调整：

- 页面整体布局
- 其他按钮
- 后端数据结构
- 依赖和构建配置

## 验收标准

- 按复现步骤重新操作，bug 不再出现。
- 不破坏相关功能：
  - 复制富文本
  - 发布检查
  - 保存作品库
  - 删除作品
- 前端改动跑 `npm run build`。
- 后端改动跑 `backend\.venv\Scripts\python.exe backend\manage.py check`。
