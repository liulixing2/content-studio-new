# 后端 / 接口需求模板

## 目标

```text
例如：新增一个文章质检接口，返回是否可发布、问题列表和修改建议。
```

## 涉及接口

```text
路径：
方法：
请求参数：
返回字段：
```

## 当前行为

```text
这里描述当前接口行为，或粘贴现有返回。
```

## 期望行为

```text
这里描述接口应该如何处理正常输入、空输入、错误输入。
```

## 数据保存规则

- 是否保存数据库：
- 是否生成版本：
- 是否只是临时结果：
- 是否需要用户确认：

## 本次边界

不要改：

- 前端布局
- 数据库结构
- 旧项目
- DeepSeek 调用

除非这里明确写需要改。

## 验收标准

- `backend\.venv\Scripts\python.exe backend\manage.py check` 通过。
- 如果有前端调用，`npm run build` 通过。
- 接口错误信息清楚。
- 不自动保存临时结果。
