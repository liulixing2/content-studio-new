# UI / 布局调整需求模板

## 目标

我想调整页面的哪个区域：

```text
例如：公众号文章页面中间主工作区。
```

## 当前现象

现在看到的问题：

```text
例如：需求区和正文区层级不清楚，按钮像普通白框堆在一起。
```

截图说明：

```text
截图文件：
红框 1：
红框 2：
要保留的区域：
要删除/移动的区域：
```

## 期望结果

我希望页面变成：

```text
例如：左侧导航保留，中间主工作区上面是需求，下面是正文，不要右侧工具栏。
```

操作流程应该是：

1. 
2. 
3. 

## 本次边界

本次只允许改：

```text
例如：只改公众号页面前端布局和样式。
```

本次不要改：

```text
例如：不要改后端接口，不要改作品库逻辑，不要接 DeepSeek。
```

## 验收标准

- 页面刷新后能看到新布局。
- 原有功能没有失效。
- `npm run build` 通过。
- 如果涉及后端，`manage.py check` 通过。
