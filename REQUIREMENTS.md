# 内容创作工作台需求说明

## 1. 项目定位

本项目是一个可独立运行的本地内容创作工作台，不依赖 Codex。

核心目标：

- 通过 DeepSeek 或手动复制模式辅助生成内容。
- 项目负责流程控制、模板排版、素材管理、质检、保存和导出。
- 所有 AI 调用必须由用户点击按钮触发，不能后台自动循环消耗额度。
- 支持本地使用，也方便后续打包给别人使用。

AI 使用模式：

- API 模式：用户填写 DeepSeek API Key，项目自动调用。
- 手动免费模式：项目生成 Prompt，用户复制到 DeepSeek 网页/App，再把结果粘回项目。

默认原则：

- 能本地处理就本地处理。
- 能手动确认就不自动消耗额度。
- 每次 AI 调用前显示用途和预计消耗。
- 自动修复、批量生成必须有次数上限。

## 2. 功能模块总览

项目分为 3 个核心功能模块：

1. 公众号文章
2. 视频包 / 剪辑
3. 小说创作

公共能力：

- 项目库：保存文章、视频包、小说项目。
- 素材库：保存上传素材、抓取候选、生成图片、配音文件。
- 模板系统：统一处理公众号排版、视频封面、小说导出格式。
- AI Provider：统一管理 DeepSeek API 模式和手动复制模式。
- 质量检查：检测模板残留、空泛内容、版权风险、缺少素材等问题。

## 3. 公众号文章模块

### 3.1 目标

生成可直接复制到公众号编辑器的富文本文章。

AI 负责内容生成，项目负责模板排版、图片插入、版权提示、保存和导出。

### 3.2 基础流程

```text
输入关键词
  ↓
DeepSeek 推荐可写方向
  ↓
用户选择方向
  ↓
DeepSeek 生成多个标题
  ↓
用户选择标题
  ↓
DeepSeek 根据标题生成结构化正文
  ↓
项目套公众号模板
  ↓
DeepSeek 生成配图方案 / 图片提示词 / 图片
  ↓
项目插入图片
  ↓
发布质检
  ↓
保存到作品库
  ↓
复制公众号富文本
```

### 3.3 输入项

- 分类：动漫、仙侠、功夫、小说、神话、武侠等。
- 风格：怀旧、盘点、单篇深挖、热点评论、对比回看。
- 关键词：用户输入，例如 `90后 动漫 龙珠`。
- 目标读者：可选，默认公众号普通读者。
- 字数范围：可选。
- 是否使用联网热点：可选。

### 3.4 DeepSeek 选题方向输出

DeepSeek 不应直接从关键词生成正文，先生成可写方向。

推荐返回结构：

```json
{
  "directions": [
    {
      "title": "方向名称",
      "reader_question": "读者会关心的问题",
      "angle": "写作角度",
      "keywords": ["关键词1", "关键词2"],
      "article_type": "single_deep / list / compare / hot_comment",
      "avoid": ["不要写成百科", "不要使用未授权截图"]
    }
  ]
}
```

### 3.5 标题生成

根据用户选择的方向生成 5-10 个标题。

标题要求：

- 具体。
- 有讨论点。
- 不出现占位词。
- 不使用“第 X 版”等无意义标题。
- 和关键词、方向一致。

### 3.6 正文生成

正文必须结构化返回，方便项目套模板。

推荐返回结构：

```json
{
  "title": "文章标题",
  "summary": "摘要",
  "sections": [
    {
      "heading": "一、...",
      "paragraphs": ["段落1", "段落2"],
      "image_hint": "这一节适合的配图方向"
    }
  ],
  "interaction": "评论区互动引导",
  "copyright_notes": ["图片不要使用影视截图"]
}
```

### 3.7 模板处理

项目负责：

- 标题样式。
- 摘要样式。
- 小标题样式。
- 段落间距。
- 图片位置。
- 互动区。
- 公众号富文本复制。
- 纯文本导出。
- HTML 导出。

AI 不负责排版。

### 3.8 配图要求

配图来源分两种：

- AI 生成原创图。
- 用户上传自有或授权素材。

默认不抓影视剧照、动漫截图、明星照片。

安全配图类型：

- 信息图。
- 文字卡。
- 剪影图。
- 抽象场景。
- 氛围图。
- 对比图。

配图流程：

```text
正文通过质检
  ↓
生成配图方案
  ↓
用户确认
  ↓
生成图片或提示词
  ↓
项目插入模板
  ↓
图片质检
```

## 4. 视频包 / 剪辑模块

### 4.1 目标

根据关键词或本地素材生成短视频剪辑包，包括素材池、剪辑方向、时间线、配音文案、字幕和导出文件。

### 4.2 素材来源

视频素材来源分两条线：

```text
素材来源
├─ 手动上传
└─ 关键词抓取
```

### 4.3 手动上传

支持上传：

- 视频：mp4、mov、mkv、webm。
- 图片：png、jpg、webp。
- 音频：mp3、wav、m4a。
- 字幕：srt、vtt。

上传后项目处理：

- 保存到素材池。
- 读取时长、分辨率、编码信息。
- 生成缩略图。
- 用户可打标签。
- 状态设为可剪辑。

### 4.4 关键词抓取

关键词抓取用于发现素材候选，不默认直接下载。

流程：

```text
输入关键词
  ↓
抓取平台候选
  ↓
展示标题、封面、平台、链接
  ↓
用户勾选
  ↓
点击下载素材
  ↓
下载成功进入素材池
  ↓
下载失败提示手动上传或换链接
```

第一版建议支持：

- B站搜索候选。
- 百度视频 / 网页候选。
- 用户粘贴直链。
- 本地上传。

微博、抖音、快手后续再扩展，因为登录、反爬、链接失效问题较多。

### 4.5 素材池状态

素材状态：

- 候选
- 下载中
- 下载成功
- 下载失败
- 已上传
- 可剪辑
- 已失效

素材字段：

```json
{
  "id": "asset_001",
  "source_type": "upload / auto_fetch / link",
  "platform": "B站 / 百度 / 本地",
  "title": "视频标题",
  "url": "原链接",
  "local_path": "本地文件路径",
  "duration": "00:02:31",
  "resolution": "1920x1080",
  "status": "可剪辑",
  "tags": ["龙珠", "90后", "热血"],
  "license_note": "用户自行确认使用权限"
}
```

### 4.6 AI 剪辑方向

DeepSeek 根据关键词、素材标题、素材标签、用户选择的素材生成剪辑方向。

输出结构：

```json
{
  "theme": "视频主题",
  "hook": "开场钩子",
  "structure": [
    {
      "part": "开场",
      "purpose": "吸引点击",
      "suggested_assets": ["asset_001"],
      "voiceover": "配音建议",
      "caption": "字幕建议"
    }
  ],
  "image_inserts": [
    {
      "type": "compare_card",
      "position": "00:00:12",
      "text": "当年看热闹，现在看后劲"
    }
  ]
}
```

### 4.7 自动剪辑

点击剪辑前必须有明确时间线。

时间线结构：

```json
[
  {
    "type": "video",
    "asset_id": "asset_001",
    "start": "00:00:10",
    "end": "00:00:18",
    "purpose": "开场钩子"
  },
  {
    "type": "image",
    "asset_id": "image_001",
    "duration": 3,
    "caption": "对比图"
  }
]
```

项目用 FFmpeg 执行剪辑。

### 4.8 配音和字幕

DeepSeek 生成配音文案。

项目负责：

- 配音文案保存。
- 字幕生成。
- TTS 配音。
- 音频合成。
- 音量处理。

TTS 可以先支持：

- 本地系统语音。
- 后续接入 edge-tts 或其他可用方案。

### 4.9 风险和限制

- AI 不能自动看懂视频画面，除非项目额外做抽帧、OCR、ASR。
- 平台视频下载不一定稳定。
- 自动抓取到的素材不一定可剪辑。
- 下载失败必须允许用户手动上传替代。
- 项目记录素材来源，是否有使用权限由用户确认。

## 5. 小说创作模块

### 5.1 目标

支持从关键词或自定义设定创建小说项目，生成方向、大纲、章节表、正文，并支持单章精修。

小说模块最适合 DeepSeek 文本能力，但必须控制上下文和调用次数。

### 5.2 两条创建路线

路线一：关键词生成方向。

```text
输入关键词
  ↓
DeepSeek 生成小说方向
  ↓
选择方向
  ↓
生成设定
  ↓
生成大纲
```

路线二：用户自定义设定。

```text
输入人物背景、世界背景、基本脉络
  ↓
DeepSeek 整理设定
  ↓
生成大纲
```

### 5.3 小说项目基础信息

- 作品名。
- 类型：仙侠、玄幻、都市、悬疑、科幻等。
- 风格：爽文、群像、慢热、悬疑、轻松等。
- 目标总字数。
- 章节数。
- 单章参考字数。
- 读者定位。

### 5.4 设定管理

项目需要保存：

- 世界观。
- 人物卡。
- 势力表。
- 境界体系。
- 主线冲突。
- 伏笔表。
- 已发生事件表。

人物卡示例：

```json
{
  "name": "陆沉",
  "age": "17",
  "identity": "外门弟子",
  "goal": "查清家族灭门真相",
  "secret": "体内封着旧神剑魂",
  "speech_style": "克制、少废话"
}
```

### 5.5 大纲生成

大纲分为：

- 总纲。
- 卷纲。
- 章节表。

章节表结构：

```json
{
  "chapter": 1,
  "title": "雨夜入宗门",
  "target_words": 2000,
  "summary": "本章剧情概要",
  "conflict": "本章冲突",
  "character_change": "人物变化",
  "hook": "结尾钩子"
}
```

### 5.6 正文生成

正文生成必须按章进行，不默认一次性生成整本。

单章生成上下文：

- 全书设定摘要。
- 当前卷纲。
- 当前章大纲。
- 前一章摘要。
- 关键人物卡。
- 本章目标字数。

### 5.7 单章精修

用户可以点开某一章，选择精修动作：

- 扩写。
- 缩写。
- 润色。
- 加强冲突。
- 加强人物心理。
- 加强爽点。
- 改对白。
- 改开头。
- 改结尾钩子。
- 修复人物不一致。
- 根据全文设定修复矛盾。

精修时不能把全文全部发送给 AI。

应发送：

- 全书设定摘要。
- 人物卡。
- 当前卷纲。
- 当前章大纲。
- 前一章摘要。
- 后一章摘要。
- 当前章节正文。
- 用户精修要求。

### 5.8 一致性检查

项目应提供：

- 人物是否崩。
- 世界规则是否冲突。
- 前后剧情是否矛盾。
- 伏笔是否回收。
- 章节是否注水。
- 字数是否达标。

### 5.9 导出

支持：

- TXT。
- Markdown。
- Word。
- 分章复制。

## 6. 数据存储建议

使用 SQLite 本地数据库。

核心表：

- projects：项目记录。
- articles：公众号文章。
- video_packages：视频包。
- assets：素材池。
- novels：小说项目。
- novel_characters：人物卡。
- novel_world_rules：世界规则。
- novel_outlines：小说大纲。
- novel_chapters：章节正文。
- ai_tasks：AI 调用记录。
- revisions：版本历史。

所有内容生成后必须保存版本，方便回退。

## 7. AI 调用控制

所有 AI 调用必须满足：

- 用户点击按钮才调用。
- 调用前显示用途。
- 自动修稿有上限。
- 批量生成要二次确认。
- 每次调用保存 Prompt 和结果。
- 失败不自动无限重试。

建议默认限制：

- 公众号文章：每篇最多 6 次 AI 调用。
- 视频包：每个视频包最多 5 次 AI 调用。
- 小说：每次只生成方向、大纲或单章，不默认批量整本。

## 8. 第一版 MVP

第一版优先实现：

### 公众号文章

- 关键词输入。
- 生成方向。
- 生成标题。
- 生成结构化正文。
- 套公众号模板。
- 生成配图方案。
- 保存和复制富文本。

### 视频包

- 手动上传素材。
- 关键词抓取候选。
- 素材池。
- AI 生成剪辑方向。
- 生成配音文案。
- 手动时间线。
- FFmpeg 导出。

### 小说

- 新建小说项目。
- 关键词生成方向。
- 自定义设定生成大纲。
- 章节表。
- 单章生成。
- 单章精修。
- TXT / Markdown 导出。

## 9. 不做或后置

第一版暂不承诺：

- 全平台稳定下载视频。
- AI 自动看懂视频画面。
- 自动批量生成整本小说。
- 无确认自动消耗 AI 额度。
- 未授权截图、剧照、明星照片自动使用。
- 网页免费版 DeepSeek 全自动控制。

## 10. 开发原则

- 新项目不引用旧项目代码。
- 旧项目只作为历史参考，不参与运行。
- 先做可控流程，再做自动化。
- 先保存结构化数据，再生成排版文件。
- 所有可消耗额度的动作都必须可见、可控、可停止。
- 本地兜底必须明确提示，不能伪装成网络热点。
